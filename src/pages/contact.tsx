import { FormContact } from "src/components/contactForm";

const contact = () => {
  return (
    <div className="min-h-screen bg-gradient-to-r from-orange-200 to-blue-500">
      <FormContact />
    </div>
  );
};

export default contact;
